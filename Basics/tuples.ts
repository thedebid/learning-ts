//A tuple is a typed array with a pre-defined length and types for each index.

let tuples: [number, boolean, string];

// initialize incorrectly throws an error
tuples = [3, true, 'Coding God was mistaken'];

console.log(tuples);
